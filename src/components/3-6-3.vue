<template>
  <h1>3.6.3 列表的更新</h1>
  <p>Vue 能够侦听响应式数组的变更方法，并在它们被调用时触发相关的更新。这些变更方法包括：<n-text code>push()</n-text>、<n-text code>pop()</n-text>、<n-text
      code>shift()</n-text>、<n-text code>unshift()</n-text>、<n-text code>splice()</n-text>、<n-text
      code>sort()</n-text>、<n-text code>reverse()</n-text>
  </p>
</template>

<script setup>
defineOptions({
    inheritAttrs: false
})
</script>